<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xiliang.mapper.OrderOutMapper">
    <select id="pageQuery" resultType="com.xiliang.entity.OrderOut">
        select * from workhouse_managementshu.order_out
        <where>
            <if test="orderId!=null">
                and order_id like concat('%',#{orderId},'%')
            </if>
        </where>
        order by create_time desc
    </select>
    <insert id="insert">
        insert into workhouse_managementshu.order_out
        (username, order_id, create_time, pay_status, goods_type, goods_num) values
        (#{username}, #{orderId}, #{createTime}, #{payStatus}, #{goodsType}, #{goodsNum})





    </insert>
</mapper>